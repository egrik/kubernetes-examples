Получить список реплика сетов:

```kubectl get replicasets.apps```

Скейлим поды по реплике сету:

```kubectl scale replicaset --replicas=3 sc--nginx``

Удаляем "все сущности"
```kubectl delete all --all```
